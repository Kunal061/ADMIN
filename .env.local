# Frontend Environment Variables
# Used by Vite - must start with VITE_ prefix

# API Base URL (using local backend proxy to avoid CORS)
# API Base URL (using local backend proxy to avoid CORS)
VITE_API_BASE_URL=/api/v1
VITE_S3_BASE_URL=https://roamania.s3.ap-south-1.amazonaws.com/images/moods
